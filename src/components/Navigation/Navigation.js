import React, { Fragment } from 'react'
import styles from '../../stylesheets/Navigation.module.css'

const Navigation = () => (
  <div className={styles.sideNav}>
    <h4>Noble Christian School</h4>
  </div>
)

export default Navigation